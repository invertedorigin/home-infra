apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opencode-pvc-replicated
  namespace: opencode
spec:
  storageClassName: ceph-rbd
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
