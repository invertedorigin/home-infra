apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-pvc-repli
  namespace: vaultwarden
spec:
  storageClassName: home-office-rack-default-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  dataSource:
    kind: PersistentVolumeClaim
    name: vaultwarden-pvc
