apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-pvc
  namespace: vaultwarden
spec:
  storageClassName: home-office-rack-default-strict-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
