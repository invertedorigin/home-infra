apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: ts-home-network
  labels:
    tailscale.com/proxy-class: "prod"
spec:
  hostname: ts-home-network
  subnetRouter:
    advertiseRoutes:
      - "10.0.1.0/24"
