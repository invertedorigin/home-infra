apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-pvc-replicated
  namespace: actual-budget
spec:
  storageClassName: home-office-rack-default-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
