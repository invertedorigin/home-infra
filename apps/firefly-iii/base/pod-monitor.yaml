apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: firefly-iii-db
  namespace: firefly-iii
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: firefly-iii-db
  podMetricsEndpoints:
  - port: metrics
