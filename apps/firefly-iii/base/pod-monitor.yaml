apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: firefly-db
  namespace: firefly
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: firefly-db
  podMetricsEndpoints:
  - port: metrics
