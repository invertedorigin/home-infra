apiVersion: v1
kind: Service
metadata:
  name: mosquitto-service
  namespace: mosquitto
spec:
  type: ClusterIP
  selector:
    app: mosquitto
  ports:
  - name: http
    port: 1883
    targetPort: 1883
