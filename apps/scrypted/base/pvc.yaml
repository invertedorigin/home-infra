apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrypted-longhorn-pvc
  namespace: scrypted
spec:
  storageClassName: longhorn-strict-local
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
