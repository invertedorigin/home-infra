apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrypted-pvc
  namespace: scrypted
spec:
  storageClassName: home-office-rack-default-strict-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
