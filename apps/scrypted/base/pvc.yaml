apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrypted-pvc
  namespace: scrypted
spec:
  storageClassName: longhorn-sticky
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
