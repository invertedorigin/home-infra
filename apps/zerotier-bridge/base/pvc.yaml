apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zerotier-pvc-replicated
  namespace: zerotier-bridge
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
