apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome-pvc-replicated
  namespace: esphome
spec:
  storageClassName: longhorn-sticky
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
