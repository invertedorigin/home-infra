apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homeassistant-longhorn-pvc
  namespace: homeassistant
spec:
  storageClassName: longhorn-strict-local
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
