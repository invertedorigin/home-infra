resources:
- namespace.yaml
- pvc.yaml
- external-secret.yaml

namespace: homeassistant
helmCharts:
- name: app-template
  repo: https://bjw-s.github.io/helm-charts/
  version: 1.5.1
  releaseName: homeassistant
  valuesInline:
    image:
      repository: homeassistant/home-assistant
      tag: 2023.10.5@sha256:adc375006da505d3f181561c2afbcf7d2493c5c742cf942b0544953857671b87

    env:
      TZ: "America/Chicago"

    service:
      main:
        ports:
          http:
            port: 8123

    ingress:
      main:
        enabled: true
        className: nginx
        annotations:
        hosts:
          - host: &host ha.invertedorigin.com
            paths:
              - path: /
                service:
                  name: main
                  port: http
        tls:
          - hosts:
              - *host

    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false 
      startup:
        enabled: false
  
    resources:
      requests:
        cpu: 50m
        memory: 200Mi
      limits:
        memory: 500Mi

    persistence:
      config:
        enabled: true
        existingClaim: homeassistant-pvc-replicated
        mountPath: /config
